summary(mod.env)
#Yield x Genotype#
mod.gen <- lm(d$Yield ~ d$Gen)
anova(mod.gen)
summary(mod.gen)
cql.temp <-
'{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "parameter_code" },
[ "00010"]
]
},
{
"op": "in",
"args": [
{ "property": "state_name" ]
["Pennsylvania"]
]
}
]
}'
temp.pa <- read_waterdata(service = "monitoring-locations",
CQL = cql.temp)
cql.temp  <-
'{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "parameter_code" },
[ "00010" ]
]
},
{
"op": "in",
"args": [
{ "property": "state_name" },
[ "Pennsylvania ]
]
}
]
}'
temp.pa <- read_waterdata(service = "monitoring-locations",
CQL = cql.temp)
rlang::last_trace()
rlang::last_trace()
cql.temp  <-
'{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "parameter_code" },
[ "00010" ]
]
},
{
"op": "in",
"args": [
{ "property": "state_name" },
[ "Pennsylvania" ]
]
}
]
}'
temp.pa <- read_waterdata(service = "monitoring-locations",
CQL = cql.temp)
cql.temp  <-
'{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "parameter_code" },
[ "00010" ]
]
},
{
"op": "in",
"args": [
{ "property": "state_name" },
[ "Pennsylvania" ]
]
}
]
}'
temp.pa <- read_waterdata(service = "monitoring-locations",
CQL = cql.temp)
rlang::last_trace()
rlang::last_trace(drop = FALSE)
cql.temp  <-
'{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "parameter_code" },
[ "00010" ]
]
},
{
"op": "in",
"args": [
{ "property": "state_name" },
[ "Pennsylvania" ]
]
}
]
}'
##Attempt at precipitation interactive  map##
#Begin with cql code#
#Make object#
#Code the interactive map in leaflet#
library(dataRetrieval)
cql.temp  <-
'{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "parameter_code" },
[ "00010" ]
]
},
{
"op": "in",
"args": [
{ "property": "state_name" },
[ "Pennsylvania" ]
]
}
]
}'
temp.pa <- read_waterdata(service = "monitoring-locations",
CQL = cql.temp)
rlang::last_trace()
pa.data <-
dataRetrieval::read_waterdata_monitoring_location(
state_name = "Pennsylvania" )
pa.data <-
dataRetrieval::read_waterdata_monitoring_location(
state_name = "Pennsylvania" )
cql.temp  <-
'{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "parameter_code" },
[ "00010" ]
]
},
{
"op": "in",
"args": [
{ "property": "state_name" },
[ "Pennsylvania" ]
]
}
]
}'
temp.pa <- read_waterdata(service = "monitoring-locations",
CQL = cql.temp)
?read_waterdata
cql.temp  <-
'{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "parameter_code" },
[ "00010" ]
]
},
{
"op": "in",
"args": [
{ "property": "state_name" },
[ "Pennsylvania" ]
]
}
]
}'
temp.pa <- read_waterdata(service = "continuous"
CQL = cql.temp)
temp.pa <- read_waterdata(service = "continuous",
CQL = cql.temp)
cql.temp  <-
'{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "parameter_code" },
[ "00010" ]
]
},
{
"op": "in",
"args": [
{ "property": "state_name" },
[ "Pennsylvania" ]
]
}
]
}'
temp.pa <- read_waterdata(service = "monitoring-locations",
CQL = cql.temp)
##Attempt at precipitation interactive  map##
#Begin with cql code#
#Make object#
parameter.codes <- parameterCdFile
View(parameter.codes)
?read_waterdata
cql.temp  <-
'{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "parameter_code" },
[ "00010" ]
]
},
{
"op": "in",
"args": [
{ "property": "monitoring_location_id" },
[ "USGS-01555000" ]
]
}
]
}'
temp.pa <- read_waterdata(service = "monitoring-locations",
CQL = cql.temp)
cql.temp  <-
'{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "parameter_code" },
[ "00010" ]
]
},
{
"op": "in",
"args": [
{ "property": "monitoring_location_id" },
[ "USGS-01555000" ]
]
}
]
}'
temp.pa <- read_waterdata(service = "monitoring-locations",
CQL = cql.temp)
cql.temp  <- '{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "parameter_code" },
[ "00010" ]
]
},
{
"op": "in",
"args": [
{ "property": "monitoring_location_id" },
[ "USGS-01555000" ]
]
}
]
}'
temp.pa <- read_waterdata(service = "monitoring-locations",
CQL = cql.temp)
cql.temp  <- '{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "state_name" },
[ "Pennsylvania" ]
]
},
{
"op": "",
"args": [in
{ "property": "parameter_code" }
["00010"]
]
}
]
}'
temp.pa <- read_waterdata(service = "monitoring-locations",
CQL = cql.temp)
cql.temp  <- '{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "state_name" },
[ "Pennsylvania" ]
]
},
{
"op": "in",
"args": [
{ "property": "parameter_code" }
["00010"]
]
}
]
}'
temp.pa <- read_waterdata(service = "monitoring-locations",
CQL = cql.temp)
req_error()
last_error()
cql.temp  <- '{
{
"op": "in",
"args": [
{ "property": "parameter_code" }
["00010"]
]
}
]
}'
temp.pa <- read_waterdata(service = "monitoring-locations",
CQL = cql.temp)
#Activate/Install Packages##
library(ggplot2)
library(dataRetrieval)
library(leaflet)
site_info <- read_waterdata_monitoring_location(monitoring_location_id = "USGS-01555000",
properties = c("monitoring_location_id",
"site_type",
"drainage_area",
"monitoring_location_name"))
sites_pa <- read_waterdata_monitoring_location(state_name = "Pennsylvania",
site_type = "Stream")
leaflet_crs <- "+proj=longlat +datum=WGS84" #default leaflet crs
leaflet(data = sites_pa |>
sf::st_transform(crs = leaflet_crs)) |>
addProviderTiles("CartoDB.Positron") |>
addCircleMarkers(popup = ~monitoring_location_name,
radius = 0.1,
opacity = 1)
#Interactive Discharge#
cql <- '{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "state_name" },
[ "Pennsylvania", "Deleware" ]
]
},
{
"op": ">",
"args": [
{ "property": "drainage_area" },
1000
]
}
]
}'
sites_pa_de <- dataRetrieval:: read_waterdata(service = "monitoring-locations",
CQL = cql)
library(leaflet)
leaflet_crs <- "+proj=longlat +datum=WGS84" #default leaflet crs
pal <- colorNumeric("viridis", sites_pa_de$drainage_area)
leaflet(data = sites_pa_de |>
sf::st_transform(crs = leaflet_crs)) |>
addProviderTiles("CartoDB.Positron") |>
addCircleMarkers(popup = ~monitoring_location_name,
color = ~ pal(drainage_area),
radius = 0.1,
opacity = 1) |>
addLegend(pal = pal,
position = "bottomleft",
title = "Drainage Area",
values = ~drainage_area)
cql_huc_wildcard <- '{
"op": "like",
"args": [
{ "property": "hydrologic_unit_code" },
"0205%"
]
}'
what_huc_sites <- dataRetrieval::read_waterdata(service = "monitoring-locations",
CQL = cql_huc_wildcard)
what_huc_sites$hydrologic_unit_code <- as.factor(what_huc_sites$hydrologic_unit_code)
pal <- colorFactor("viridis", what_huc_sites$hydrologic_unit_code)
leaflet(data = what_huc_sites |>
sf::st_transform(crs = leaflet_crs)) |>
addProviderTiles("CartoDB.Positron") |>
addCircleMarkers(popup = ~monitoring_location_name,
color = ~ pal(hydrologic_unit_code),
radius = 0.1,
opacity = 1)
cql <- '{
"op": "and",
"args": [
{
"op": "in",
"args": [
{ "property": "state_name" },
[ "Pennsylvania" ]
]
},
{
"op": ">",
"args": [
{ "property": "altitude" },
100
]
}
]
}'
leaflet_crs <- "+proj=longlat +datum=WGS84" #default leaflet crs
pal <- colorNumeric("viridis", sites_pa$altitude)
leaflet(data = sites_pa |>
sf::st_transform(crs = leaflet_crs)) |>
addProviderTiles("CartoDB.Positron") |>
addCircleMarkers(popup = ~monitoring_location_name,
color = ~ pal(altitude),
radius = 0.1,
opacity = 1) |>
addLegend(pal = pal,
position = "bottomleft",
title = "altitude",
values = ~altitude)
data <- read.csv("Dryad_data.csv")
# Take a look at what we are working with:
head(data)
# Starting with $A6F03 the rest of these columns all represent a measure of loci diversity.
#Let's see if this measure of diversity is evenly distributed across the landscape or has a geographic pattern.
#First we will create a linear model with the lm() function.
# The $UTM.X column is the UTM version of longitude (i.e. east-west range)
# It follows the same pattern as our plot() function with y~x so:
mod.lon <- lm(data$epi3 ~ data$UTM.X..huso.30.)
# I've given the object a name that starts with "mod" to remind myself it's a model result
# Now let's see if the "epi3" locus is significantly predicted by the longitude of the population it came from.
# The anova() function is a more general function that tests the significance of model results:
anova(mod.lon)
# You can use the summary() function for a more detailed view of the model results:
summary(mod.lon)
# Maybe there is a pattern by latitude - many species and genes follow predictable north-south patterns with climate.
mod.lat <- lm(data$epi3~data$UTM.Y..huso.30.)
anova(mod.lat)
summary(mod.lat)
data <- read.csv("Dryad_data.csv")
# Make sure to set your working directory then read the data into R. If these next two lines generate an error, then you do not have GitHub setup properly on your computer.
# I can not stress this enough - do NOT change these lines if they don't work - change your GitHub directory setup.
setwd("C:/GitHub/hornam/Week4")
data <- read.csv("Dryad_data.csv")
# Take a look at what we are working with:
head(data)
# Starting with $A6F03 the rest of these columns all represent a measure of loci diversity.
#Let's see if this measure of diversity is evenly distributed across the landscape or has a geographic pattern.
#First we will create a linear model with the lm() function.
# The $UTM.X column is the UTM version of longitude (i.e. east-west range)
# It follows the same pattern as our plot() function with y~x so:
mod.lon <- lm(data$epi3 ~ data$UTM.X..huso.30.)
# I've given the object a name that starts with "mod" to remind myself it's a model result
# Now let's see if the "epi3" locus is significantly predicted by the longitude of the population it came from.
# The anova() function is a more general function that tests the significance of model results:
anova(mod.lon)
# You can use the summary() function for a more detailed view of the model results:
summary(mod.lon)
# Maybe there is a pattern by latitude - many species and genes follow predictable north-south patterns with climate.
mod.lat <- lm(data$epi3~data$UTM.Y..huso.30.)
anova(mod.lat)
summary(mod.lat)
# So latitude and longitude individually don't influence the genotype. But what about lat and lon together?
# To look at the interaction between these two values we can use the multiplication symbol -> '*'
mod.latlon <- lm(data$epi3 ~ data$UTM.Y..huso.30. * data$UTM.X..huso.30.)
anova(mod.latlon)
summary(mod.latlon)
# Additionally, if you want to view how sample locations relate to each other, you can use a scatter plot pretending one predicts the other:
plot(data$UTM.Y..huso.30. ~ data$UTM.X..huso.30.)
# We still use the same linear model. But the complicated bit is how it deals with factors to compare group means.
# This post is a GREAT explanation of the math: https://www.r-bloggers.com/the-lm-function-with-categorical-predictors/
# Think of it as a one-way ANOVA for several groups, and the syntax is the same:
mod.fac <- lm(data$epi3~data$Region)
anova(mod.fac)
summary(mod.fac)
plot(data$UTM.Y..huso.30. ~ data$UTM.X..huso.30., col = factor(data$Region), pch=19)
#Great, except we don't know which region is which! We need to add a legend to the plot.Just make sure the legend color scheme matches the points.
#Unfortunately if we look at the group names, someone included slashes! Because of this, R thinks they are "escape characters" and will not print a legend
levels(as.factor(data$Region))
plot(data$UTM.Y..huso.30. ~ data$UTM.X..huso.30., col = factor(data$Region), pch= c(11:29)[as.integer(as.factor(data$Region))])
#Feel free to explore this dataset with other comparisons of alleles and regions.
